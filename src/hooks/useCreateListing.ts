
import { useCreateListing as useCreateListingImplementation } from "./listings/useCreateListing";

// Re-export the hook for backward compatibility
export const useCreateListing = useCreateListingImplementation;
