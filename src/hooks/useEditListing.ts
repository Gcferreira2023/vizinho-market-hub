
import { useEditListing as useEditListingImplementation } from "./listings/useEditListing";

// Re-export the hook for backward compatibility
export const useEditListing = useEditListingImplementation;
